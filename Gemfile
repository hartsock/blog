source "https://rubygems.org"

# GitHub Pages builds this site with the github-pages gem, which pins Jekyll and
# bundles minima, jekyll-seo-tag, jekyll-feed, and jekyll-sitemap. Listing the
# gem here lets you reproduce the GitHub Pages build locally with `bundle exec
# jekyll serve`.
gem "github-pages", group: :jekyll_plugins
